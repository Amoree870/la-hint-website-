# La Hint — شاشة الملف الشخصي

واجهة جوال لمقهى **La Hint**، مبنية بـ HTML و CSS و JavaScript خام مع **Vite** كأداة تجميع.
التصميم داكن بألوان دافئة، مستوحى من شاشات الملف الشخصي في iOS مع عناصر زجاجية (glassmorphism).

الواجهة **عربية بالكامل (RTL)** ما عدا اسم المقهى `La Hint` فيبقى بالإنجليزية.

## لمحة نخيل القصيم في الهوية البصرية

- **سعف النخيل بدل أكاليل الغار**: الشكلان اللذان يحيطان بالاسم سعفتان مرسومتان
  توليديًا (SVG) بتدرّج ذهبي/تمري، بدل صور الغار الأصلية.
- **نقشة نخيل خافتة** خلف إطار الجوال (شفافية 6٪) تعطي إحساس البستان دون إزعاج بصري.
- **لون ذهبي مستخلص من التمر** (`--gold: #e0bc73`) يظهر في وهج زر الإنجازات وفي أيقونة الكأس.
- **الأرقام بالخط العربي الهندي** (١٥٤ / ٣٦ / ١٢) لاتساق الهوية.
- **زر التبديل** في بطاقة المفضل يمرّ على مشروبات منها *لاتيه التمر السكري*
  و*قهوة عربية بالهيل*.

## التشغيل

```bash
npm install
npm run dev      # خادم التطوير
npm run build    # بناء الإنتاج في dist/
npm run preview  # معاينة ناتج البناء
```

## التجربة على الآيفون

### الطريقة الأولى: خادم التطوير عبر الشبكة المحلية (تحتاج حاسبًا)

```bash
npm install
npm run dev -- --host
```

يطبع Vite عنوانًا شبيهًا بـ `http://192.168.x.x:5173`. افتحه في Safari على الآيفون
مع بقاء الجهازين على نفس شبكة الواي فاي.

### الطريقة الثانية: GitHub Pages (بلا حاسب)

المستودع فيه `.github/workflows/deploy.yml`. فعّل النشر مرة واحدة:
**Settings → Pages → Source = GitHub Actions**. بعد أول دفعة سيصبح الموقع على
`https://<user>.github.io/la-hint-website-/` وتفتحه من الجوال مباشرة.

### على الجهاز الفعلي

- عند عرض ≤ 500px بلمس، يختفي إطار المحاكاة وتملأ الشاشةُ الجهازَ كتطبيق حقيقي،
  مع احترام المناطق الآمنة (`env(safe-area-inset-*)`) للنوتش وشريط الإيماءة.
- «مشاركة → إضافة إلى الشاشة الرئيسية» يشغّله بملء الشاشة بلا شريط عناوين.
- iOS يشغّل الفيديو تلقائيًا فقط وهو مكتوم (`muted` + `playsinline`، وكلاهما مضبوط).
  في وضع توفير الطاقة يمنعه النظام، ولذلك يعيد `main.js` المحاولة عند أول لمسة.

## البنية

```
index.html                    الهيكل (lang="ar" dir="rtl")
src/
  main.js                     نقطة الدخول: الخطوط، الزجاج السائل، الفيديو، زر التبديل
  style.css                   كامل التنسيقات والحركات ومتغيرات الألوان
  liquid-glass.js             توليد خرائط الإزاحة ومرشحات SVG للزجاج
public/assets/images/         سعف النخيل والنقشة والأيقونات (SVG)
tools/generate-palm-assets.mjs  مولّد أصول النخيل (node tools/generate-palm-assets.mjs)
```

## الزجاج السائل (`liquid-glass.js`)

لكل عنصر يحمل `data-liquid`:

1. تُقاس أبعاده ونصف قطر حوافه.
2. تُبنى خريطة إزاحة على `<canvas>` باستخدام **دالة المسافة الموقّعة (SDF)** لمستطيل
   مُدوّر الأركان؛ يُخزَّن متجه الانكسار في القناتين `R` و`G` ويتركّز عند الحافة.
3. تُنشأ مرشحات `<feImage> + <feDisplacementMap>` وتُطبَّق عبر
   `backdrop-filter: url(#id) blur(0.3px) saturate(1.3)`.
4. إن لم يدعم المتصفح مرشحات SVG داخل `backdrop-filter` (فايرفوكس/سفاري — ومنها
   Safari على الآيفون) يُكتفى بالتمويه العادي المعرّف في CSS.

### حدّان مقصودان في التنفيذ

- **`minSize: 48`** — العناصر التي يقلّ أصغر أبعادها عن ٤٨px تبقى على تمويه CSS.
  على زر 58×44 يقترب شريط الانكسار من نصف ارتفاع الزر فينقلب الأثر إلى تكبير
  عنيف بحافّة حادّة داخل الزر (رُصد ذلك في كروم فوق واجهة الفيديو). الحبّة
  الكبيرة (225×54) تتجاوز الحد فتحصل على الانكسار الحقيقي.
- **`animation-fill-mode: backwards`** وليس `both` — كل حركة تنتهي عند الحالة
  الطبيعية للعنصر، ولو بقي `transform: translateY(0)` مطبَّقًا بعد الانتهاء
  (وهذا ما يفعله `both`) لرُفع العنصر إلى طبقة تركيب منفصلة فانزاح فضاء
  إحداثيات المرشح وظهرت مستطيلات حادّة داخل الأزرار.

## ملاحظات على الأصول

- **الخطوط**: خطا *Neue Haas Unica* و*Helvetica Now Display* تجاريان وغير مرفقين
  بالمستودع، ولا يغطيان العربية أصلًا. البديل المستخدم هو
  **IBM Plex Sans Arabic** محمّلًا **محليًا** عبر `@fontsource` (يعمل بلا إنترنت)،
  مع بقاء الخطين الأصليين في سلسلة الاحتياط للنص اللاتيني.
- **الأيقونات وسعف النخيل**: أُنتجت كـ **SVG** بدل PNG (أوضح عند أي تكبير وأخف حجمًا)
  وتقع في `public/assets/images/`.
- **الصور البعيدة والفيديو** تُحمَّل من روابطها الأصلية كما هي في المواصفة.

## الاستجابة وإمكانية الوصول

- عرض 440px فأقل: `zoom: 0.6` للإطار، والجسم يلتفّ عند 900px.
- جميع الحركات مُعطَّلة تحت `prefers-reduced-motion: reduce`.
- الأزرار عناصر `button` بتسميات `aria-label` عربية وحلقة تركيز ذهبية.

---

# BREZZA — موقع تأجير كيترنق القهوة والتيراميسو

المصدر: **`public/brezza/index.html`** — والموقع منشور على:

**https://amoree870.github.io/la-hint-website-/**

## خريطة الروابط بعد النشر

| الرابط | الصفحة |
|---|---|
| `/` | موقع BREZZA (الصفحة الرئيسية) |
| `/brezza/` | نفس موقع BREZZA — رابط بديل يبقى شغّالًا |
| `/la-hint.html` | شاشة الملف الشخصي La Hint |

الترتيب يتم في `tools/arrange-dist.mjs` الذي يعمل تلقائيًا بعد `vite build`.
تطبيق La Hint مبنيّ بـ `base: './'` فيبقى ملفه في جذر `dist` (باسم `la-hint.html`)
ولا يُنقل إلى مجلد فرعي — لأن نقله يكسر مسارات `./assets`.

النشر يجري عبر GitHub Actions عند كل دفعة على `main`.

موقع تعريفي عربي كامل (RTL) لمشروع **بريزا**، بهوية بصرية مطابقة لعربة القهوة
المرفقة: وردي خوخي `#F5A08B`، أصفر شمسي `#FFD11A`، وبرقوقي داكن `#7B4B5D`،
مع نخلة الشعار.

**نموذج العمل:** بريزا تؤجّر **المعدات** وتوصّل **القهوة والتيراميسو** جاهزين —
والتقديم للضيوف مسؤولية العميل. لا يوجد باريستا ولا طاقم تقديم، وكل نصوص
الموقع مكتوبة على هذا الأساس.

## المواصفات

- **ملف HTML واحد مستقل** — كل الـ CSS والـ JS داخله، بلا أي أداة بناء.
  يفتح مباشرة بالمتصفح أو يُرفع على أي استضافة.
- موضوع في `public/` كي ينسخه Vite كما هو إلى `dist/brezza/` عند البناء،
  فيصير على GitHub Pages تحت المسار `/brezza/`.

## الأقسام

الواجهة الرئيسية · شريط متحرك · من نحن · الخدمات (٤ خدمات + ما يشمله التجهيز) ·
آلية العمل · الباقات · الأرقام · آراء العملاء · موقعنا (عنيزة وبريدة) ·
الأسئلة الشائعة · نموذج الحجز · الفوتر.

## التفاعل

- ظهور العناصر عند التمرير عبر `IntersectionObserver`
- عدّادات رقمية متحرّكة بالأرقام العربية الهندية
- شريط تقدّم للتمرير + تتبّع القسم النشط في القائمة
- إمالة ثلاثية الأبعاد للعربة ولبطاقات الخدمات مع حركة المؤشر
- أكورديون للأسئلة الشائعة (يفتح واحدًا ويغلق البقية)
- قائمة جوال بملء الشاشة
- **دورة صبّ قهوة متحرّكة** في رسم العربة: خيط قهوة ينزل من الصنبور، الكوب
  يمتلئ تدريجيًا، ثم يُستبدل بكوب فارغ وتبدأ الدورة من جديد (٥٫٢ ثانية)
- **نموذج الحجز يبني رسالة واتساب جاهزة** من مدخلات المستخدم
- كل الحركات مُعطَّلة تحت `prefers-reduced-motion: reduce` (ويستقرّ رسم العربة
  عندها على كوب نصف ممتلئ بلا خيط صبّ بدل أن يتجمّد في منتصف الحركة)

## بيانات التواصل

مضبوطة في ثوابت أعلى الـ `<script>`، وتُزامَن منها **كل** روابط الصفحة تلقائيًا
عند التحميل — فيكفي تعديلها من مكان واحد:

```js
var WHATSAPP  = '966595497479';   // بصيغة دولية بلا + وبلا صفر
var PHONE     = '+966595497479';
var INSTAGRAM = 'brezza.sa';
```

أيقونتا **واتساب** و**إنستقرام** تظهران بشعاريهما وألوانهما الحقيقية (أخضر واتساب،
وتدرّج إنستقرام) في ثلاثة مواضع، وكلها روابط تفتح الحساب مباشرة: قائمة التواصل في
قسم الحجز، والفوتر، وزرّان عائمان ثابتان أسفل الشاشة (مع تلميح باسم الحساب/الرقم
عند المرور على سطح المكتب).

## ما زال يحتاج تعديلًا قبل النشر

| ماذا | أين |
|---|---|
| العناوين على الخرائط | روابط `maps.google.com` في قسم «موقعنا» |
| آراء العملاء والأرقام | أقسام «قالوا عنّا» و«الأرقام» — نصوص توضيحية |

## الخطوط

`Cairo` و`Baloo Bhaijaan 2` من Google Fonts عبر `<link>`. لو انقطع الإنترنت
يسقط الموقع تلقائيًا على خطوط النظام العربية دون كسر التخطيط.
